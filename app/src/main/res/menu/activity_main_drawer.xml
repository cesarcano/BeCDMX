<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <item android:title="@string/lugares"><item
        android:id="@+id/nav_sabiasque"
        android:icon="@drawable/ic_menu_share"
        android:title="@string/festivales" />
        <menu>
            <item
                android:id="@+id/nav_festivales"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/festivales" />
            <item
                android:id="@+id/nav_ferias"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/ferias" />
            <item
                android:id="@+id/nav_festividades"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/festividades" />
            <item
                android:id="@+id/nav_museos"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/museos" />
            <item
                android:id="@+id/nav_auditorios"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/auditorios" />
            <item
                android:id="@+id/nav_bibliotecas"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/bibliotecas" />
            <item
                android:id="@+id/nav_centros"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/centros_culturales" />
            <item
                android:id="@+id/nav_galerias"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/galerias" />
        </menu>
    </item>
</menu>
